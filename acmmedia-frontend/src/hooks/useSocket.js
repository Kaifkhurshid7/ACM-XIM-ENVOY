/**
 * Socket Connection Hook
 * 
 * Re-exports the useSocket hook from SocketContext for convenience.
 * Provides access to the Socket.IO client instance.
 * 
 * @module hooks/useSocket
 */

export { useSocket } from "../context/SocketContext";
